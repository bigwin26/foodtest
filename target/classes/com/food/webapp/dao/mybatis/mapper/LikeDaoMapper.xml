<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<<<<<<< HEAD

<mapper namespace="com.food.webapp.dao.LikeDao">
	
	<insert id="Insert" parameterType="com.food.webapp.entity.Liked">	
		insert into Liked(restaurantId,memberId)
		values(#{restaurantId},#{memberId})
	</insert>
		
	<select id="Check" resultType="java.lang.Integer">
		select count(*)
		from Liked where restaurantId = #{restaurantId} and memberId = #{memberId}
	</select>
	
 	<delete id="delete" parameterType="java.lang.Integer">
		delete from Liked where restaurantId = #{restaurantId} and memberId = #{memberId}
	</delete> 
</mapper>
=======
    
    <mapper namespace="com.food.webapp.dao.LikeDao">
    
    <insert id="like" parameterType="com.food.webapp.entity.Like">
    insert into Liked(restaurantId, memberId) value(#{restaurantId}, #{memberId})
    
	</insert>
    
    <delete id="unlike" parameterType="com.food.webapp.entity.Like">
    
    delete from Liked where restaurantId = #{restaurantId} and memberId #{memberId}
    </delete>
    
    
    </mapper>
>>>>>>> refs/remotes/origin/master
