<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.food.webapp.dao.RestaurantDao">

	<select id="getList" parameterType="com.food.webapp.entity.Restaurant">
		SELECT * FROM Restaurant where ${param2} like '%${param3}%' order by regDate desc limit ${(page-1)*10}, 10
	</select>

	<select id="count">
		SELECT count(*) count FROM Restaurant
	</select>

	<insert id="insert" parameterType="com.food.webapp.entity.Restaurant">
		<selectKey order="BEFORE" keyProperty="id" resultType="int">
			select IFNULL(max(cast(id as unsigned)), 0)+1 from Restaurant
		</selectKey>
		insert into Restaurant(id, name, location, address, content, genre, keyword, image, tip)
		value(#{id},#{name}, #{location}, #{address},#{content},#{genre},#{keyword},#{image},#{tip});
	</insert>

</mapper>